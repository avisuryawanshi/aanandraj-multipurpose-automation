package appiumguide;

public class InstallAPKByADBCommand {

    /**
     * ✅ 2. Using ADB Command (Manual/Scripted Install)
     * You can use adb install to push the APK to the device before launching Appium:
     *
     * bash
     * adb install -r path/to/your/app.apk
     *         • -r flag reinstalls if the app already exists.
     *         • Useful for pre-installed apps or when debugging install issues.
     */
}
